# ==============================================================================
# SEED Labs 2.0 — Web Server Container Dockerfile (CSRF Legitimate Site)
#
# Base Image: handsonsecurity/seed-server:apache-php
#   - Serves the trusted Elgg social network application (www.seed-server.com).
# ==============================================================================

FROM handsonsecurity/seed-server:apache-php

# Copy target application source into web root
COPY src /var/www/html
